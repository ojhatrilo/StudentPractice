1.
    mini project on college Grade system 
    if the mark is 90+ print the grade O
    if the mark is 80+ print the grade A
    if the mark is 70+ print the grade B
    if the mark is 60+ print the grade C
    if the mark is 50+ print the grade D
    if the mark is 40+ or less than 50 print the grade U
    and mention that you are fail
2.
    write the program about the gusseing number
    ask the user to enter the number 
    and check the number entered by the user matches with seceret number or not
    if it is matches the seceret number declare that you are win
    if it doesnot matches declare that you are loose
3.
    write a program to find the given number is odd or even
4.
    write the program about the fizzbuzz
    if the number is divisible by 3 print(Fizz)
    if the number is divisible by 5 print(buzz)
    if the number is divisible by 3 and 5 print(Fizzbuzz)

